# MCP Toolbox Configuration - Gasco Invoice System
# Configuración para dual-project architecture con herramientas de ZIP

sources:
  # Fuente para lectura de facturas (datalake-gasco)
  gasco_invoices_read:
    kind: bigquery
    project: datalake-gasco
    location: us-central1

  # Fuente para operaciones de ZIP (agent-intelligence-gasco)
  gasco_operations_write:
    kind: bigquery
    project: agent-intelligence-gasco
    location: us-central1

tools:
  # === HERRAMIENTAS DE BÚSQUEDA DE FACTURAS ===
  
  search_invoices:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        Factura,
        Solicitante,
        Rut,
        Nombre,
        DetallesFactura,
        Copia_Tributaria_cf,
        Copia_Cedible_cf,
        Copia_Tributaria_sf,
        Copia_Cedible_sf,
        Doc_Termico
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
      ORDER BY Factura DESC
      LIMIT 100
    description: |
      Busca y recupera información básica de facturas del dataset de producción de Gasco.
      Retorna hasta 10 facturas con campos esenciales incluyendo todas las rutas de archivos PDF disponibles.
      Los PDFs están en campos separados: Copia_Tributaria_cf, Copia_Cedible_cf, Copia_Tributaria_sf, Copia_Cedible_sf, Doc_Termico.

  search_invoices_by_date:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        Factura,
        Solicitante,
        Rut,
        Nombre,
        fecha,
        DetallesFactura,
        CASE 
          WHEN Copia_Tributaria_cf IS NOT NULL 
          THEN Copia_Tributaria_cf
          ELSE NULL
        END as Copia_Tributaria_cf_proxy,
        CASE 
          WHEN Copia_Cedible_cf IS NOT NULL 
          THEN Copia_Cedible_cf
          ELSE NULL
        END as Copia_Cedible_cf_proxy,
        CASE 
          WHEN Copia_Tributaria_sf IS NOT NULL 
          THEN Copia_Tributaria_sf
          ELSE NULL
        END as Copia_Tributaria_sf_proxy,
        CASE 
          WHEN Copia_Cedible_sf IS NOT NULL 
          THEN Copia_Cedible_sf
          ELSE NULL
        END as Copia_Cedible_sf_proxy,
        CASE 
          WHEN Doc_Termico IS NOT NULL 
          THEN Doc_Termico
          ELSE NULL
        END as Doc_Termico_proxy
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
      WHERE
        fecha = @target_date
      ORDER BY Factura DESC
      LIMIT 10
    description: |
      Busca facturas de una fecha específica con enlaces de descarga del proxy local.
      Proporciona la fecha en formato YYYY-MM-DD.
      Retorna facturas con URLs del proxy local listas para descargar.
      Útil para consultas como "Facturas del 26 de diciembre de 2019" o "Facturas del 2022-11-04".
    parameters:
      - name: target_date
        type: string
        description: Fecha específica en formato YYYY-MM-DD (ej. "2019-12-26", "2022-11-04")

  search_invoices_by_rut:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        Factura,
        Solicitante,
        Rut,
        Nombre,
        fecha,
        DetallesFactura,
        CASE 
          WHEN Copia_Tributaria_cf IS NOT NULL 
          THEN Copia_Tributaria_cf
          ELSE NULL
        END as Copia_Tributaria_cf_proxy,
        CASE 
          WHEN Copia_Cedible_cf IS NOT NULL 
          THEN Copia_Cedible_cf
          ELSE NULL
        END as Copia_Cedible_cf_proxy,
        CASE 
          WHEN Copia_Tributaria_sf IS NOT NULL 
          THEN Copia_Tributaria_sf
          ELSE NULL
        END as Copia_Tributaria_sf_proxy,
        CASE 
          WHEN Copia_Cedible_sf IS NOT NULL 
          THEN Copia_Cedible_sf
          ELSE NULL
        END as Copia_Cedible_sf_proxy,
        CASE 
          WHEN Doc_Termico IS NOT NULL 
          THEN Doc_Termico
          ELSE NULL
        END as Doc_Termico_proxy
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
      WHERE
        Rut = @target_rut
      ORDER BY Factura DESC
      LIMIT 1000
    description: |
      Busca facturas de un RUT específico del cliente con enlaces de descarga del proxy local.
      Proporciona el RUT en formato con guión.
      Retorna facturas con URLs del proxy local listas para descargar.
      Útil para consultas como "Facturas del RUT 9025012-4" o "Facturas de María Torres".
    parameters:
      - name: target_rut
        type: string
        description: RUT del cliente en formato con guión (ej. "9025012-4", "76341146-K")

  search_invoices_by_date_range:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        Factura,
        Solicitante,
        Rut,
        Nombre,
        fecha,
        DetallesFactura,
        CASE 
          WHEN Copia_Tributaria_cf IS NOT NULL 
          THEN Copia_Tributaria_cf
          ELSE NULL
        END as Copia_Tributaria_cf_proxy,
        CASE 
          WHEN Copia_Cedible_cf IS NOT NULL 
          THEN Copia_Cedible_cf
          ELSE NULL
        END as Copia_Cedible_cf_proxy,
        CASE 
          WHEN Copia_Tributaria_sf IS NOT NULL 
          THEN Copia_Tributaria_sf
          ELSE NULL
        END as Copia_Tributaria_sf_proxy,
        CASE 
          WHEN Copia_Cedible_sf IS NOT NULL 
          THEN Copia_Cedible_sf
          ELSE NULL
        END as Copia_Cedible_sf_proxy,
        CASE 
          WHEN Doc_Termico IS NOT NULL 
          THEN Doc_Termico
          ELSE NULL
        END as Doc_Termico_proxy
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
      WHERE
        fecha >= @start_date
        AND fecha <= @end_date
      ORDER BY fecha DESC, Factura DESC
      LIMIT 1000
    description: |
      Busca facturas dentro de un rango de fechas específico con enlaces de descarga del proxy local.
      Proporciona fechas en formato YYYY-MM-DD.
      Retorna facturas con URLs del proxy local listas para descargar.
      Útil para consultas como "Facturas entre 2019-12-01 y 2019-12-31".
    parameters:
      - name: start_date
        type: string
        description: Fecha de inicio en formato YYYY-MM-DD (ej. "2019-12-01")
      - name: end_date
        type: string
        description: Fecha de fin en formato YYYY-MM-DD (ej. "2019-12-31")

  search_invoices_by_rut_and_date_range:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        Factura,
        Solicitante,
        Rut,
        Nombre,
        fecha,
        DetallesFactura,
        CASE 
          WHEN Copia_Tributaria_cf IS NOT NULL 
          THEN Copia_Tributaria_cf
          ELSE NULL
        END as Copia_Tributaria_cf_proxy,
        CASE 
          WHEN Copia_Cedible_cf IS NOT NULL 
          THEN Copia_Cedible_cf
          ELSE NULL
        END as Copia_Cedible_cf_proxy,
        CASE 
          WHEN Copia_Tributaria_sf IS NOT NULL 
          THEN Copia_Tributaria_sf
          ELSE NULL
        END as Copia_Tributaria_sf_proxy,
        CASE 
          WHEN Copia_Cedible_sf IS NOT NULL 
          THEN Copia_Cedible_sf
          ELSE NULL
        END as Copia_Cedible_sf_proxy,
        CASE 
          WHEN Doc_Termico IS NOT NULL 
          THEN Doc_Termico
          ELSE NULL
        END as Doc_Termico_proxy
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
      WHERE
        Rut = @target_rut
        AND fecha >= @start_date
        AND fecha <= @end_date
      ORDER BY fecha DESC, Factura DESC
      LIMIT 15
    description: |
      Busca facturas de un RUT específico dentro de un rango de fechas con enlaces de descarga del proxy local.
      Combina filtrado por RUT del cliente y rango de fechas específico.
      Proporciona el RUT en formato con guión y fechas en formato YYYY-MM-DD.
      Retorna facturas con URLs del proxy local listas para descargar.
      Útil para consultas como "Facturas del RUT 9025012-4 en diciembre 2019" o "RUT 76341146-K entre 2022-11-01 y 2022-11-30".
    parameters:
      - name: target_rut
        type: string
        description: RUT del cliente en formato con guión (ej. "9025012-4", "76341146-K")
      - name: start_date
        type: string
        description: Fecha de inicio en formato YYYY-MM-DD (ej. "2019-12-01")
      - name: end_date
        type: string
        description: Fecha de fin en formato YYYY-MM-DD (ej. "2019-12-31")

  get_solicitantes_by_rut:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        DISTINCT Solicitante,
        COUNT(*) as factura_count,
        MIN(fecha) as fecha_primera_factura,
        MAX(fecha) as fecha_ultima_factura,
        MAX(Nombre) as nombre_cliente
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
      WHERE
        Rut = @target_rut
      GROUP BY Solicitante
      ORDER BY factura_count DESC, Solicitante ASC
      LIMIT 10
    description: |
      Obtiene todos los códigos de solicitante (SAP) asociados a un RUT específico.
      Muestra estadísticas para cada solicitante: cantidad de facturas, rango temporal y nombre del cliente.
      Útil para consultas como "qué solicitantes pertenecen al RUT 96568740-8" o "códigos SAP del RUT X".
      Ordena por cantidad de facturas (descendente) para mostrar los solicitantes más activos primero.
    parameters:
      - name: target_rut
        type: string
        description: RUT del cliente en formato con guión (ej. "96568740-8", "76341146-K")

  search_invoices_by_month_year:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        Factura,
        Solicitante,
        Rut,
        Nombre,
        fecha,
        DetallesFactura,
        CASE 
          WHEN Copia_Tributaria_cf IS NOT NULL 
          THEN Copia_Tributaria_cf
          ELSE NULL
        END as Copia_Tributaria_cf_proxy,
        CASE 
          WHEN Copia_Cedible_cf IS NOT NULL 
          THEN Copia_Cedible_cf
          ELSE NULL
        END as Copia_Cedible_cf_proxy,
        CASE 
          WHEN Copia_Tributaria_sf IS NOT NULL 
          THEN Copia_Tributaria_sf
          ELSE NULL
        END as Copia_Tributaria_sf_proxy,
        CASE 
          WHEN Copia_Cedible_sf IS NOT NULL 
          THEN Copia_Cedible_sf
          ELSE NULL
        END as Copia_Cedible_sf_proxy,
        CASE 
          WHEN Doc_Termico IS NOT NULL 
          THEN Doc_Termico
          ELSE NULL
        END as Doc_Termico_proxy
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
      WHERE
        EXTRACT(YEAR FROM fecha) = @target_year
        AND EXTRACT(MONTH FROM fecha) = @target_month
      ORDER BY fecha DESC, Factura DESC
      LIMIT 50
    description: |
      Busca facturas de un mes y año específicos con enlaces de descarga del proxy local.
      Utiliza funciones EXTRACT para filtrar por mes y año de forma precisa.
      Proporciona el año como entero (ej. 2019) y el mes como entero (1-12).
      Retorna facturas con URLs del proxy local listas para descargar.
      Útil para consultas como "Facturas de diciembre 2019" o "Facturas de noviembre 2022".
    parameters:
      - name: target_year
        type: integer
        description: Año de las facturas (ej. 2019, 2022, 2025)
      - name: target_month
        type: integer
        description: Mes de las facturas (1=enero, 2=febrero, ..., 12=diciembre)

  search_invoices_by_multiple_ruts:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        Factura,
        Solicitante,
        Rut,
        Nombre,
        fecha,
        DetallesFactura,
        CASE 
          WHEN Copia_Tributaria_cf IS NOT NULL 
          THEN Copia_Tributaria_cf
          ELSE NULL
        END as Copia_Tributaria_cf_proxy,
        CASE 
          WHEN Copia_Cedible_cf IS NOT NULL 
          THEN Copia_Cedible_cf
          ELSE NULL
        END as Copia_Cedible_cf_proxy,
        CASE 
          WHEN Copia_Tributaria_sf IS NOT NULL 
          THEN Copia_Tributaria_sf
          ELSE NULL
        END as Copia_Tributaria_sf_proxy,
        CASE 
          WHEN Copia_Cedible_sf IS NOT NULL 
          THEN Copia_Cedible_sf
          ELSE NULL
        END as Copia_Cedible_sf_proxy,
        CASE 
          WHEN Doc_Termico IS NOT NULL 
          THEN Doc_Termico
          ELSE NULL
        END as Doc_Termico_proxy
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
      WHERE
        Rut IN UNNEST(SPLIT(@rut_list, ','))
      ORDER BY Rut, fecha DESC, Factura DESC
      LIMIT 1000
    description: |
      Busca facturas de múltiples RUTs específicos con enlaces de descarga del proxy local.
      Utiliza funciones UNNEST y SPLIT para procesar una lista de RUTs separados por comas.
      Proporciona los RUTs como string separados por comas, sin espacios adicionales.
      Retorna facturas con URLs del proxy local listas para descargar, agrupadas por RUT.
      Útil para consultas como "Facturas de los RUTs 9025012-4,76341146-K" o "Facturas de María Torres y Luis Gutiérrez".
    parameters:
      - name: rut_list
        type: string
        description: Lista de RUTs separados por comas sin espacios (ej. "9025012-4,76341146-K,4911410-9")

  search_invoices_recent_by_date:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        Factura,
        Solicitante,
        Rut,
        Nombre,
        fecha,
        DetallesFactura,
        CASE 
          WHEN Copia_Tributaria_cf IS NOT NULL 
          THEN Copia_Tributaria_cf
          ELSE NULL
        END as Copia_Tributaria_cf_proxy,
        CASE 
          WHEN Copia_Cedible_cf IS NOT NULL 
          THEN Copia_Cedible_cf
          ELSE NULL
        END as Copia_Cedible_cf_proxy,
        CASE 
          WHEN Copia_Tributaria_sf IS NOT NULL 
          THEN Copia_Tributaria_sf
          ELSE NULL
        END as Copia_Tributaria_sf_proxy,
        CASE 
          WHEN Copia_Cedible_sf IS NOT NULL 
          THEN Copia_Cedible_sf
          ELSE NULL
        END as Copia_Cedible_sf_proxy,
        CASE 
          WHEN Doc_Termico IS NOT NULL 
          THEN Doc_Termico
          ELSE NULL
        END as Doc_Termico_proxy
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
      ORDER BY fecha DESC, Factura DESC
      LIMIT @limit_count
    description: |
      Busca las facturas más recientes del sistema con enlaces de descarga del proxy local.
      Ordena por fecha descendente (más reciente primero) y luego por número de factura.
      Permite especificar el número máximo de resultados a retornar.
      Retorna facturas con URLs del proxy local listas para descargar.
      Útil para consultas como "Las 10 facturas más recientes" o "Últimas 5 facturas".
    parameters:
      - name: limit_count
        type: integer
        description: Número máximo de facturas a retornar (ej. 5, 10, 20)

  search_invoices_by_proveedor:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        Factura,
        Solicitante,
        Rut,
        Nombre,
        DetallesFactura,
        Copia_Tributaria_cf,
        Copia_Cedible_cf,
        Copia_Tributaria_sf,
        Copia_Cedible_sf,
        Doc_Termico
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
      WHERE
        UPPER(Solicitante) LIKE CONCAT('%', UPPER(@proveedor_name), '%')
      ORDER BY Factura DESC
      LIMIT 10
    description: |
      Busca facturas por nombre de proveedor/solicitante.
      Proporciona parte del nombre del proveedor y encontrará facturas coincidentes.
      Retorna facturas con todas las rutas de archivos PDF disponibles.
    parameters:
      - name: proveedor_name
        type: string
        description: Nombre completo o parcial del proveedor/solicitante (ej. "AGROSUPER", "Embotelladora")

  # === NUEVAS HERRAMIENTAS PARA BÚSQUEDA ESPECÍFICA POR FACTURA Y REFERENCIA ===
  
  search_invoices_by_factura_number:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        Factura,
        Factura_Referencia,
        Solicitante,
        Rut,
        Nombre,
        fecha,
        DetallesFactura,
        CASE 
          WHEN Copia_Tributaria_cf IS NOT NULL 
          THEN Copia_Tributaria_cf
          ELSE NULL
        END as Copia_Tributaria_cf_proxy,
        CASE 
          WHEN Copia_Cedible_cf IS NOT NULL 
          THEN Copia_Cedible_cf
          ELSE NULL
        END as Copia_Cedible_cf_proxy,
        CASE 
          WHEN Copia_Tributaria_sf IS NOT NULL 
          THEN Copia_Tributaria_sf
          ELSE NULL
        END as Copia_Tributaria_sf_proxy,
        CASE 
          WHEN Copia_Cedible_sf IS NOT NULL 
          THEN Copia_Cedible_sf
          ELSE NULL
        END as Copia_Cedible_sf_proxy,
        CASE 
          WHEN Doc_Termico IS NOT NULL 
          THEN Doc_Termico
          ELSE NULL
        END as Doc_Termico_proxy
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
      WHERE
        Factura = @factura_number
        OR LTRIM(Factura, '0') = LTRIM(@factura_number, '0')
      ORDER BY Factura DESC
      LIMIT 5
    description: |
      Busca facturas específicamente por el campo FACTURA (ID interno del sistema).
      Utiliza búsqueda exacta y también búsqueda sin ceros iniciales para mayor flexibilidad.
      Retorna facturas con URLs del proxy local listas para descargar.
      Usar cuando el usuario pregunte específicamente por "factura" o "ID de factura".
    parameters:
      - name: factura_number
        type: string
        description: Número de factura del campo Factura (ID interno), con o sin ceros iniciales

  search_invoices_by_referencia_number:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        Factura,
        Factura_Referencia,
        Solicitante,
        Rut,
        Nombre,
        fecha,
        DetallesFactura,
        CASE 
          WHEN Copia_Tributaria_cf IS NOT NULL 
          THEN Copia_Tributaria_cf
          ELSE NULL
        END as Copia_Tributaria_cf_proxy,
        CASE 
          WHEN Copia_Cedible_cf IS NOT NULL 
          THEN Copia_Cedible_cf
          ELSE NULL
        END as Copia_Cedible_cf_proxy,
        CASE 
          WHEN Copia_Tributaria_sf IS NOT NULL 
          THEN Copia_Tributaria_sf
          ELSE NULL
        END as Copia_Tributaria_sf_proxy,
        CASE 
          WHEN Copia_Cedible_sf IS NOT NULL 
          THEN Copia_Cedible_sf
          ELSE NULL
        END as Copia_Cedible_sf_proxy,
        CASE 
          WHEN Doc_Termico IS NOT NULL 
          THEN Doc_Termico
          ELSE NULL
        END as Doc_Termico_proxy
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
      WHERE
        Factura_Referencia = @referencia_number
        OR LTRIM(Factura_Referencia, '0') = LTRIM(@referencia_number, '0')
      ORDER BY Factura DESC
      LIMIT 5
    description: |
      Busca facturas específicamente por el campo FACTURA_REFERENCIA (número visible en la factura).
      Utiliza búsqueda exacta y también búsqueda sin ceros iniciales para mayor flexibilidad.
      Retorna facturas con URLs del proxy local listas para descargar.
      Usar cuando el usuario pregunte por "referencia", "número de referencia" o el número que aparece en la factura impresa.
    parameters:
      - name: referencia_number
        type: string
        description: Número de referencia del campo Factura_Referencia (el que aparece en la factura), con o sin ceros iniciales

  search_invoices_by_any_number:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        Factura,
        Factura_Referencia,
        Solicitante,
        Rut,
        Nombre,
        fecha,
        DetallesFactura,
        CASE 
          WHEN Copia_Tributaria_cf IS NOT NULL 
          THEN Copia_Tributaria_cf
          ELSE NULL
        END as Copia_Tributaria_cf_proxy,
        CASE 
          WHEN Copia_Cedible_cf IS NOT NULL 
          THEN Copia_Cedible_cf
          ELSE NULL
        END as Copia_Cedible_cf_proxy,
        CASE 
          WHEN Copia_Tributaria_sf IS NOT NULL 
          THEN Copia_Tributaria_sf
          ELSE NULL
        END as Copia_Tributaria_sf_proxy,
        CASE 
          WHEN Copia_Cedible_sf IS NOT NULL 
          THEN Copia_Cedible_sf
          ELSE NULL
        END as Copia_Cedible_sf_proxy,
        CASE 
          WHEN Doc_Termico IS NOT NULL 
          THEN Doc_Termico
          ELSE NULL
        END as Doc_Termico_proxy,
        CASE 
          WHEN Factura = @search_number OR LTRIM(Factura, '0') = LTRIM(@search_number, '0') THEN 'FACTURA'
          WHEN Factura_Referencia = @search_number OR LTRIM(Factura_Referencia, '0') = LTRIM(@search_number, '0') THEN 'REFERENCIA'
          ELSE 'UNKNOWN'
        END as match_type
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
      WHERE
        Factura = @search_number
        OR Factura_Referencia = @search_number
        OR LTRIM(Factura, '0') = LTRIM(@search_number, '0')
        OR LTRIM(Factura_Referencia, '0') = LTRIM(@search_number, '0')
      ORDER BY 
        CASE 
          WHEN Factura = @search_number THEN 1
          WHEN Factura_Referencia = @search_number THEN 2
          WHEN LTRIM(Factura, '0') = LTRIM(@search_number, '0') THEN 3
          ELSE 4
        END,
        Factura DESC
      LIMIT 5
    description: |
      Busca facturas en AMBOS campos (Factura Y Factura_Referencia) simultáneamente.
      Útil cuando no se sabe si el número corresponde al campo Factura o Factura_Referencia.
      Incluye un campo match_type que indica en qué campo se encontró la coincidencia.
      Ordena los resultados priorizando coincidencias exactas.
      Usar cuando el usuario proporcione un número sin especificar si es factura o referencia.
    parameters:
      - name: search_number
        type: string
        description: Número a buscar en ambos campos (Factura y Factura_Referencia), con o sin ceros iniciales

  search_invoices_by_cliente:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        Factura,
        Solicitante,
        Rut,
        Nombre,
        DetallesFactura,
        Copia_Tributaria_cf,
        Copia_Cedible_cf,
        Copia_Tributaria_sf,
        Copia_Cedible_sf,
        Doc_Termico
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
      WHERE
        UPPER(Nombre) LIKE CONCAT('%', UPPER(@cliente_name), '%')
      ORDER BY Factura DESC
      LIMIT 10
    description: |
      Busca facturas por nombre de cliente.
      Proporciona parte del nombre del cliente y encontrará facturas coincidentes.
      Retorna facturas con todas las rutas de archivos PDF disponibles.
    parameters:
      - name: cliente_name
        type: string
        description: Nombre completo o parcial del cliente

  search_invoices_by_minimum_amount:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        Factura,
        Solicitante,
        Rut,
        Nombre,
        DetallesFactura,
        Copia_Tributaria_cf,
        Copia_Cedible_cf,
        Copia_Tributaria_sf,
        Copia_Cedible_sf,
        Doc_Termico,
        (SELECT SUM(CAST(detalle.ImporteTotal AS FLOAT64)) 
         FROM UNNEST(DetallesFactura) as detalle) as total_amount
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
      WHERE
        (SELECT SUM(CAST(detalle.ImporteTotal AS FLOAT64)) 
         FROM UNNEST(DetallesFactura) as detalle) >= @min_amount
      ORDER BY total_amount DESC
      LIMIT 10
    description: |
      Busca facturas con un monto total mínimo.
      Proporciona el monto mínimo como un número.
      Retorna facturas con todas las rutas de archivos PDF disponibles ordenadas por monto (mayor primero).
    parameters:
      - name: min_amount
        type: float
        description: Monto total mínimo para la factura (ej. 1000.0)

  get_invoice_statistics:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        COUNT(*) as total_facturas,
        COUNT(DISTINCT Rut) as proveedores_unicos,
        COUNT(DISTINCT Nombre) as clientes_unicos,
        COUNT(DISTINCT Factura) as facturas_unicas,
        MIN(Factura) as factura_mas_antigua,
        MAX(Factura) as factura_mas_reciente,
        COUNT(CASE WHEN Copia_Tributaria_cf IS NOT NULL THEN 1 END) as facturas_con_pdf_cf,
        COUNT(CASE WHEN Copia_Tributaria_sf IS NOT NULL THEN 1 END) as facturas_con_pdf_sf,
        AVG(ARRAY_LENGTH(DetallesFactura)) as promedio_lineas_por_factura
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
    description: |
      Obtiene estadísticas comprensivas sobre todas las facturas.
      Retorna conteos, totales, promedios, rangos de números de factura para el dataset de facturas.
      Incluye estadísticas sobre disponibilidad de PDFs por tipo.

  get_invoices_with_pdf_info:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        Factura,
        Solicitante,
        Rut,
        Nombre,
        DetallesFactura,
        Copia_Tributaria_cf,
        Copia_Cedible_cf,
        Copia_Tributaria_sf,
        Copia_Cedible_sf,
        Doc_Termico
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
      WHERE
        (COALESCE(@invoice_numbers, '') = '' OR Factura IN UNNEST(SPLIT(@invoice_numbers, ',')))
      ORDER BY Factura DESC
      LIMIT 25
    description: |
      Obtiene información completa de facturas incluyendo todas las rutas de archivos PDF disponibles.
      Puede filtrar por números de factura específicos o retornar todas las facturas.
      Los PDFs están disponibles en campos separados por tipo de copia.
      El agente debe usar estos datos para generar enlaces de descarga manualmente.
    parameters:
      - name: invoice_numbers
        type: string
        description: Lista opcional de números de factura para filtrar (separados por comas). Si está vacío, retorna todas las facturas.
        required: false

  get_invoices_with_proxy_links:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        Factura,
        Solicitante,
        Rut,
        Nombre,
        CONCAT('https://invoice-backend-819133916464.us-central1.run.app/invoice/', Factura, '.pdf') as proxy_download_url,
        CASE
          WHEN Copia_Tributaria_cf IS NOT NULL THEN 'Disponible'
          ELSE 'No disponible'
        END as pdf_tributaria_status,
        CASE
          WHEN Copia_Cedible_cf IS NOT NULL THEN 'Disponible'
          ELSE 'No disponible'
        END as pdf_cedible_status
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
      WHERE
        (COALESCE(@solicitante_code, '') = '' OR Solicitante = @solicitante_code)
      ORDER BY Factura DESC
      LIMIT 25
    description: |
      Obtiene facturas con URLs del proxy local ya formateadas para descargas directas.
      Esta herramienta genera automáticamente los enlaces de descarga correctos.
      Úsala cuando necesites proveer enlaces de descarga listos para usar.
    parameters:
      - name: solicitante_code
        type: string
        description: Código de solicitante específico (ej. "0012148561"). Opcional.
        required: false

  get_invoices_with_all_pdf_links:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        Factura,
        Solicitante,
        Rut,
        Nombre,
        CASE 
          WHEN Copia_Tributaria_cf IS NOT NULL 
          THEN Copia_Tributaria_cf
          ELSE NULL
        END as tributaria_cf_url,
        CASE 
          WHEN Copia_Cedible_cf IS NOT NULL 
          THEN Copia_Cedible_cf
          ELSE NULL
        END as cedible_cf_url,
        CASE 
          WHEN Copia_Tributaria_sf IS NOT NULL 
          THEN Copia_Tributaria_sf
          ELSE NULL
        END as tributaria_sf_url,
        CASE 
          WHEN Copia_Cedible_sf IS NOT NULL 
          THEN Copia_Cedible_sf
          ELSE NULL
        END as cedible_sf_url,
        CASE 
          WHEN Doc_Termico IS NOT NULL 
          THEN Doc_Termico
          ELSE NULL
        END as termico_url,
        CONCAT(
          CASE WHEN Copia_Tributaria_cf IS NOT NULL THEN '✅ Copia Tributaria (con fondo - logo Gasco)\n' ELSE '' END,
          CASE WHEN Copia_Cedible_cf IS NOT NULL THEN '✅ Copia Cedible (con fondo - logo Gasco)\n' ELSE '' END,
          CASE WHEN Copia_Tributaria_sf IS NOT NULL THEN '✅ Copia Tributaria (sin fondo - sin logo)\n' ELSE '' END,
          CASE WHEN Copia_Cedible_sf IS NOT NULL THEN '✅ Copia Cedible (sin fondo - sin logo)\n' ELSE '' END,
          CASE WHEN Doc_Termico IS NOT NULL THEN '✅ Documento Térmico\n' ELSE '' END
        ) as pdfs_disponibles
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
      WHERE
        Solicitante = LPAD(@solicitante_code, 10, '0')
      ORDER BY Factura DESC
      LIMIT 25
    description: |
      Obtiene facturas con TODOS los enlaces específicos de PDFs disponibles para un solicitante específico.
      Genera URLs directas de GCS para cada tipo de PDF: Tributaria CF/SF, Cedible CF/SF, Documento Térmico.
      Usa esta herramienta cuando necesites mostrar TODOS los PDFs disponibles para facturas de un solicitante.
      Incluye un resumen visual de los PDFs disponibles.
      CRÍTICO: Siempre requiere solicitante_code - NO retorna todas las facturas sin filtro.
    parameters:
      - name: solicitante_code
        type: string
        description: Código de solicitante específico (ej. "0012537749"). REQUERIDO.
        required: true

  get_multiple_pdf_downloads:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        Factura,
        Solicitante,
        Rut,
        Nombre,
        -- Enlaces directos para cada tipo de PDF
        CASE WHEN Copia_Tributaria_cf IS NOT NULL 
             THEN Copia_Tributaria_cf 
             ELSE NULL END as tributaria_con_firma_url,
        CASE WHEN Copia_Cedible_cf IS NOT NULL 
             THEN Copia_Cedible_cf 
             ELSE NULL END as cedible_con_firma_url,
        CASE WHEN Copia_Tributaria_sf IS NOT NULL 
             THEN Copia_Tributaria_sf 
             ELSE NULL END as tributaria_sin_firma_url,
        CASE WHEN Copia_Cedible_sf IS NOT NULL 
             THEN Copia_Cedible_sf 
             ELSE NULL END as cedible_sin_firma_url,
        CASE WHEN Doc_Termico IS NOT NULL 
             THEN Doc_Termico 
             ELSE NULL END as documento_termico_url,
        -- Contador de PDFs disponibles
        (CASE WHEN Copia_Tributaria_cf IS NOT NULL THEN 1 ELSE 0 END +
         CASE WHEN Copia_Cedible_cf IS NOT NULL THEN 1 ELSE 0 END +
         CASE WHEN Copia_Tributaria_sf IS NOT NULL THEN 1 ELSE 0 END +
         CASE WHEN Copia_Cedible_sf IS NOT NULL THEN 1 ELSE 0 END +
         CASE WHEN Doc_Termico IS NOT NULL THEN 1 ELSE 0 END) as total_pdfs_disponibles,
        -- Lista descriptiva de PDFs disponibles
        CONCAT(
          CASE WHEN Copia_Tributaria_cf IS NOT NULL THEN 'Copia Tributaria (Con Fondo - logo Gasco) ' ELSE '' END,
          CASE WHEN Copia_Cedible_cf IS NOT NULL THEN 'Copia Cedible (Con Fondo - logo Gasco) ' ELSE '' END,
          CASE WHEN Copia_Tributaria_sf IS NOT NULL THEN 'Copia Tributaria (Sin Fondo - sin logo) ' ELSE '' END,
          CASE WHEN Copia_Cedible_sf IS NOT NULL THEN 'Copia Cedible (Sin Fondo - sin logo) ' ELSE '' END,
          CASE WHEN Doc_Termico IS NOT NULL THEN 'Documento Termico ' ELSE '' END
        ) as tipos_pdf_disponibles
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
      WHERE
        (COALESCE(@solicitante_code, '') = '' OR Solicitante = @solicitante_code)
      ORDER BY Factura DESC
    description: |
      HERRAMIENTA ESPECIALIZADA: Obtiene TODOS los PDFs disponibles para un solicitante específico.
      
      Muestra cada tipo de PDF con URL directa funcional:
      - tributaria_con_fondo_url: Copia Tributaria con fondo (logo Gasco)
      - cedible_con_fondo_url: Copia Cedible con fondo (logo Gasco)
      - tributaria_sin_fondo_url: Copia Tributaria sin fondo (sin logo)
      - cedible_sin_fondo_url: Copia Cedible sin fondo (sin logo)
      - documento_termico_url: Documento Térmico
      
      Incluye contador total_pdfs_disponibles y lista tipos_pdf_disponibles.
      
      Úsala cuando el usuario pregunte por facturas de un solicitante específico y necesites mostrar TODAS las opciones de PDF.
    parameters:
      - name: solicitante_code
        type: string
        description: Código de solicitante específico (ej. "0012148561")
        required: true

  # === HERRAMIENTAS DE GESTIÓN DE ZIPS ===

  create_zip_record:
    kind: bigquery-sql
    source: gasco_operations_write
    statement: |
      INSERT INTO `agent-intelligence-gasco.zip_operations.zip_files`
      (zip_id, filename, facturas, status, gcs_path, size_bytes, metadata)
      VALUES
      (@zip_id, @filename, @facturas, @status, @gcs_path, @size_bytes, PARSE_JSON(@metadata))
    description: |
      Crea un registro de archivo ZIP en la base de datos.
      Usado internamente para tracking de ZIPs generados.
    parameters:
      - name: zip_id
        type: string
        description: ID único del archivo ZIP
      - name: filename
        type: string
        description: Nombre del archivo ZIP
      - name: facturas
        type: string
        description: Lista de números de factura incluidos en el ZIP (separados por comas)
      - name: status
        type: string
        description: Estado del ZIP (created, processing, ready, error)
      - name: gcs_path
        type: string
        description: Ruta completa en Google Cloud Storage
      - name: size_bytes
        type: integer
        description: Tamaño del archivo en bytes
      - name: metadata
        type: string
        description: Metadatos adicionales en formato JSON

  list_zip_files:
    kind: bigquery-sql
    source: gasco_operations_write
    statement: |
      SELECT
        zip_id,
        filename,
        facturas,
        created_at,
        status,
        gcs_path,
        size_bytes,
        metadata
      FROM
        `agent-intelligence-gasco.zip_operations.zip_files`
      ORDER BY created_at DESC
      LIMIT 10
    description: |
      Lista los archivos ZIP más recientes generados por el sistema.
      Útil para mostrar historial de descargas al usuario.

  get_zip_info:
    kind: bigquery-sql
    source: gasco_operations_write
    statement: |
      SELECT
        zip_id,
        filename,
        facturas,
        created_at,
        status,
        gcs_path,
        size_bytes,
        metadata
      FROM
        `agent-intelligence-gasco.zip_operations.zip_files`
      WHERE
        zip_id = @zip_id
    description: |
      Obtiene información detallada de un archivo ZIP específico.
      Usado para verificar estado y obtener URL de descarga.
    parameters:
      - name: zip_id
        type: string
        description: ID único del archivo ZIP

  update_zip_status:
    kind: bigquery-sql
    source: gasco_operations_write
    statement: |
      UPDATE `agent-intelligence-gasco.zip_operations.zip_files`
      SET status = @new_status,
          size_bytes = @size_bytes,
          gcs_path = @gcs_path
      WHERE zip_id = @zip_id
    description: |
      Actualiza el estado y información de un archivo ZIP.
      Usado durante el proceso de generación de ZIP.
    parameters:
      - name: zip_id
        type: string
        description: ID único del archivo ZIP
      - name: new_status
        type: string
        description: Nuevo estado del ZIP
      - name: size_bytes
        type: integer
        description: Tamaño final del archivo en bytes
      - name: gcs_path
        type: string
        description: Ruta final en Google Cloud Storage

  record_zip_download:
    kind: bigquery-sql
    source: gasco_operations_write
    statement: |
      INSERT INTO `agent-intelligence-gasco.zip_operations.zip_downloads`
      (zip_id, client_ip, user_agent, success)
      VALUES
      (@zip_id, @client_ip, @user_agent, @success)
    description: |
      Registra una descarga de archivo ZIP para analytics.
      Usado para tracking de uso y estadísticas.
    parameters:
      - name: zip_id
        type: string
        description: ID único del archivo ZIP descargado
      - name: client_ip
        type: string
        description: Dirección IP del cliente
      - name: user_agent
        type: string
        description: User agent del navegador
      - name: success
        type: boolean
        description: Si la descarga fue exitosa

  get_zip_statistics:
    kind: bigquery-sql
    source: gasco_operations_write
    statement: |
      SELECT
        COUNT(*) as total_zips_created,
        COUNT(CASE WHEN status = 'ready' THEN 1 END) as zips_ready,
        COUNT(CASE WHEN status = 'error' THEN 1 END) as zips_error,
        SUM(size_bytes) as total_size_bytes,
        AVG(size_bytes) as average_size_bytes,
        COUNT(DISTINCT DATE(created_at)) as days_with_activity,
        (SELECT COUNT(*) FROM `agent-intelligence-gasco.zip_operations.zip_downloads`) as total_downloads
      FROM
        `agent-intelligence-gasco.zip_operations.zip_files`
    description: |
      Obtiene estadísticas comprensivas sobre la actividad de ZIPs.
      Útil para monitoreo y reporting del sistema.

  get_cedible_cf_by_solicitante:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        Factura,
        Solicitante,
        Rut,
        Nombre,
        Copia_Cedible_cf as cedible_cf_url,
        'Copia Cedible Con Fondo (logo Gasco)' as tipo_documento
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
      WHERE
        Solicitante = @solicitante_code
        AND Copia_Cedible_cf IS NOT NULL
      ORDER BY Factura DESC
      LIMIT 10
    description: |
      Obtiene facturas Cedible CON FONDO (logo Gasco) para un solicitante específico.
      Solo retorna facturas que tienen disponible la Copia Cedible Con Fondo.
      Usar cuando el usuario pida específicamente "cedible con fondo" o "cedible cf".
    parameters:
      - name: solicitante_code
        type: string
        description: Código de solicitante específico (ej. "0012148561")
        required: true

  get_cedible_sf_by_solicitante:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        Factura,
        Solicitante,
        Rut,
        Nombre,
        Copia_Cedible_sf as cedible_sf_url,
        'Copia Cedible Sin Fondo (sin logo)' as tipo_documento
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
      WHERE
        Solicitante = @solicitante_code
        AND Copia_Cedible_sf IS NOT NULL
      ORDER BY Factura DESC
      LIMIT 10
    description: |
      Obtiene facturas Cedible SIN FONDO (sin logo) para un solicitante específico.
      Solo retorna facturas que tienen disponible la Copia Cedible Sin Fondo.
      Usar cuando el usuario pida específicamente "cedible sin fondo" o "cedible sf".
    parameters:
      - name: solicitante_code
        type: string
        description: Código de solicitante específico (ej. "0012148561")
        required: true

  get_tributaria_cf_by_solicitante:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        Factura,
        Solicitante,
        Rut,
        Nombre,
        Copia_Tributaria_cf as tributaria_cf_url,
        'Copia Tributaria Con Fondo (logo Gasco)' as tipo_documento
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
      WHERE
        Solicitante = @solicitante_code
        AND Copia_Tributaria_cf IS NOT NULL
      ORDER BY Factura DESC
      LIMIT 10
    description: |
      Obtiene facturas Tributaria CON FONDO (logo Gasco) para un solicitante específico.
      Solo retorna facturas que tienen disponible la Copia Tributaria Con Fondo.
      Usar cuando el usuario pida específicamente "tributaria con fondo" o "tributaria cf".
    parameters:
      - name: solicitante_code
        type: string
        description: Código de solicitante específico (ej. "0012148561")
        required: true

  get_tributaria_sf_by_solicitante:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        Factura,
        Solicitante,
        Rut,
        Nombre,
        Copia_Tributaria_sf as tributaria_sf_url,
        'Copia Tributaria Sin Fondo (sin logo)' as tipo_documento
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
      WHERE
        Solicitante = @solicitante_code
        AND Copia_Tributaria_sf IS NOT NULL
      ORDER BY Factura DESC
      LIMIT 10
    description: |
      Obtiene facturas Tributaria SIN FONDO (sin logo) para un solicitante específico.
      Solo retorna facturas que tienen disponible la Copia Tributaria Sin Fondo.
      Usar cuando el usuario pida específicamente "tributaria sin fondo" o "tributaria sf".
    parameters:
      - name: solicitante_code
        type: string
        description: Código de solicitante específico (ej. "0012148561")
        required: true

  get_tributarias_by_solicitante:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        Factura,
        Solicitante,
        Rut,
        Nombre,
        CASE WHEN Copia_Tributaria_cf IS NOT NULL 
             THEN Copia_Tributaria_cf 
             ELSE NULL END as tributaria_cf_url,
        CASE WHEN Copia_Tributaria_sf IS NOT NULL 
             THEN Copia_Tributaria_sf 
             ELSE NULL END as tributaria_sf_url,
        CONCAT(
          CASE WHEN Copia_Tributaria_cf IS NOT NULL THEN 'Tributaria Con Fondo (logo Gasco) ' ELSE '' END,
          CASE WHEN Copia_Tributaria_sf IS NOT NULL THEN 'Tributaria Sin Fondo (sin logo) ' ELSE '' END
        ) as tipos_tributarios_disponibles,
        (CASE WHEN Copia_Tributaria_cf IS NOT NULL THEN 1 ELSE 0 END +
         CASE WHEN Copia_Tributaria_sf IS NOT NULL THEN 1 ELSE 0 END) as total_tributarias_disponibles
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
      WHERE
        Solicitante = @solicitante_code
        AND (Copia_Tributaria_cf IS NOT NULL OR Copia_Tributaria_sf IS NOT NULL)
      ORDER BY Factura DESC
      LIMIT 10
    description: |
      Obtiene TODAS las facturas TRIBUTARIAS (con y sin firma) para un solicitante específico.
      Solo retorna facturas que tienen al menos una copia tributaria disponible.
      Muestra tanto tributaria_cf_url como tributaria_sf_url cuando están disponibles.
      Usar cuando el usuario pida "facturas tributarias" o "todas las tributarias".
    parameters:
      - name: solicitante_code
        type: string
        description: Código de solicitante específico (ej. "0012148561")
        required: true

  get_cedibles_by_solicitante:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        Factura,
        Solicitante,
        Rut,
        Nombre,
        CASE WHEN Copia_Cedible_cf IS NOT NULL 
             THEN Copia_Cedible_cf 
             ELSE NULL END as cedible_cf_url,
        CASE WHEN Copia_Cedible_sf IS NOT NULL 
             THEN Copia_Cedible_sf 
             ELSE NULL END as cedible_sf_url,
        CONCAT(
          CASE WHEN Copia_Cedible_cf IS NOT NULL THEN 'Cedible Con Fondo (logo Gasco) ' ELSE '' END,
          CASE WHEN Copia_Cedible_sf IS NOT NULL THEN 'Cedible Sin Fondo (sin logo) ' ELSE '' END
        ) as tipos_cedibles_disponibles,
        (CASE WHEN Copia_Cedible_cf IS NOT NULL THEN 1 ELSE 0 END +
         CASE WHEN Copia_Cedible_sf IS NOT NULL THEN 1 ELSE 0 END) as total_cedibles_disponibles
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
      WHERE
        Solicitante = @solicitante_code
        AND (Copia_Cedible_cf IS NOT NULL OR Copia_Cedible_sf IS NOT NULL)
      ORDER BY Factura DESC
      LIMIT 10
    description: |
      Obtiene TODAS las facturas CEDIBLES (con y sin firma) para un solicitante específico.
      Solo retorna facturas que tienen al menos una copia cedible disponible.
      Muestra tanto cedible_cf_url como cedible_sf_url cuando están disponibles.
      Usar cuando el usuario pida "facturas cedibles" o "todas las cedibles".
    parameters:
      - name: solicitante_code
        type: string
        description: Código de solicitante específico (ej. "0012148561")
        required: true

  search_invoices_by_solicitante_and_date_range:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        Factura,
        Solicitante,
        Rut,
        Nombre,
        fecha,
        DetallesFactura,
        CASE WHEN Copia_Cedible_cf IS NOT NULL 
             THEN Copia_Cedible_cf 
             ELSE NULL END as Copia_Cedible_cf_proxy,
        CASE WHEN Copia_Cedible_sf IS NOT NULL 
             THEN Copia_Cedible_sf 
             ELSE NULL END as Copia_Cedible_sf_proxy,
        CASE WHEN Copia_Tributaria_cf IS NOT NULL 
             THEN Copia_Tributaria_cf 
             ELSE NULL END as Copia_Tributaria_cf_proxy,
        CASE WHEN Copia_Tributaria_sf IS NOT NULL 
             THEN Copia_Tributaria_sf 
             ELSE NULL END as Copia_Tributaria_sf_proxy,
        CASE WHEN Doc_Termico IS NOT NULL 
             THEN Doc_Termico 
             ELSE NULL END as Doc_Termico_proxy
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
      WHERE
        Solicitante = LPAD(@solicitante, 10, '0') AND fecha BETWEEN @start_date AND @end_date
      ORDER BY fecha DESC, Factura DESC
      LIMIT 25
    description: |
      Busca facturas por código de solicitante específico y rango de fechas.
      Permite filtrar por empresa/organización solicitante en un período determinado.
      El código SAP/solicitante se normaliza automáticamente con ceros a la izquierda (10 dígitos).
      Útil para consultas como "facturas del SAP 12148561" (se busca como "0012148561").
      Retorna hasta 50 facturas ordenadas por fecha descendente.
    parameters:
      - name: solicitante
        type: string
        description: Código SAP/solicitante (ej. "12148561", "0012148561"). Se normaliza automáticamente con ceros leading.
        required: true
      - name: start_date
        type: string
        description: Fecha de inicio en formato YYYY-MM-DD (ej. "2019-01-01")
        required: true
      - name: end_date
        type: string
        description: Fecha de fin en formato YYYY-MM-DD (ej. "2019-12-31")
        required: true

  get_unique_ruts_statistics:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        Rut,
        COUNT(*) as total_facturas,
        MIN(fecha) as primera_factura,
        MAX(fecha) as ultima_factura,
        COUNT(DISTINCT Solicitante) as solicitantes_distintos
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
      WHERE
        Rut IS NOT NULL AND Rut != ''
      GROUP BY Rut
      HAVING COUNT(*) >= @min_facturas
      ORDER BY total_facturas DESC, ultima_factura DESC
      LIMIT @limit_ruts
    description: |
      Obtiene estadísticas completas de RUTs únicos en el sistema.
      Muestra cantidad de facturas, rango temporal, solicitantes distintos y disponibilidad de PDFs.
      Útil para análisis de clientes más activos y patrones de facturación.
      Permite filtrar por cantidad mínima de facturas y limitar resultados.
    parameters:
      - name: min_facturas
        type: integer
        description: Cantidad mínima de facturas que debe tener un RUT para aparecer en los resultados (default 1)
        required: false
        default: 1
      - name: limit_ruts
        type: integer
        description: Número máximo de RUTs a retornar (default 50)
        required: false
        default: 50

  search_invoices_by_rut_and_amount:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      WITH factura_totales AS (
        SELECT
          Factura,
          Rut,
          Nombre,
          Solicitante,
          fecha,
          Copia_Cedible_cf,
          Copia_Cedible_sf,
          Copia_Tributaria_cf,
          Copia_Tributaria_sf,
          Doc_Termico,
          COALESCE(
            (SELECT SUM(CAST(REGEXP_REPLACE(CAST(detalle.ValorTotal AS STRING), '[^0-9.-]', '') AS FLOAT64))
             FROM UNNEST(detallesFactura) AS detalle 
             WHERE detalle.ValorTotal IS NOT NULL 
             AND REGEXP_CONTAINS(CAST(detalle.ValorTotal AS STRING), r'^-?[0-9]+\.?[0-9]*$')), 0
          ) as valor_total_calculado
        FROM
          `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
        WHERE
          Rut = @target_rut
      )
      SELECT
        Factura,
        Rut,
        Nombre,
        Solicitante,
        fecha,
        valor_total_calculado,
        CASE WHEN Copia_Cedible_cf IS NOT NULL 
             THEN Copia_Cedible_cf 
             ELSE NULL END as Copia_Cedible_cf_proxy,
        CASE WHEN Copia_Cedible_sf IS NOT NULL 
             THEN Copia_Cedible_sf 
             ELSE NULL END as Copia_Cedible_sf_proxy,
        CASE WHEN Copia_Tributaria_cf IS NOT NULL 
             THEN Copia_Tributaria_cf 
             ELSE NULL END as Copia_Tributaria_cf_proxy,
        CASE WHEN Copia_Tributaria_sf IS NOT NULL 
             THEN Copia_Tributaria_sf 
             ELSE NULL END as Copia_Tributaria_sf_proxy,
        CASE WHEN Doc_Termico IS NOT NULL 
             THEN Doc_Termico 
             ELSE NULL END as Doc_Termico_proxy,
        REGEXP_EXTRACT(Copia_Tributaria_cf, r'[^/]+$') AS archivo_pdf_nombre
      FROM
        factura_totales
      WHERE
        valor_total_calculado >= @min_amount
      ORDER BY valor_total_calculado DESC, fecha DESC
      LIMIT 10
    description: |
      Busca facturas de un RUT específico que superen un monto mínimo.
      Calcula el valor total sumando todos los ítems de detallesFactura.
      Útil para encontrar facturas de alto valor de un cliente específico.
      Incluye URLs proxy para descarga directa de todos los tipos de PDF.
    parameters:
      - name: target_rut
        type: string
        description: RUT específico a buscar (ej. "9025012-4", "76341146-K")
        required: true
      - name: min_amount
        type: integer
        description: Monto mínimo en pesos chilenos (ej. 100000 para facturas sobre $100.000)
        required: true

  get_date_range_statistics:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        DATE(fecha) as fecha_factura,
        COUNT(*) as total_facturas,
        COUNT(DISTINCT Rut) as ruts_distintos,
        COUNT(DISTINCT Solicitante) as solicitantes_distintos,
        COUNT(DISTINCT Nombre) as clientes_distintos,
        COUNT(CASE WHEN Copia_Tributaria_cf IS NOT NULL THEN 1 END) as facturas_con_tributaria_cf,
        COUNT(CASE WHEN Copia_Cedible_cf IS NOT NULL THEN 1 END) as facturas_con_cedible_cf,
        COUNT(CASE WHEN Copia_Tributaria_sf IS NOT NULL THEN 1 END) as facturas_con_tributaria_sf,
        COUNT(CASE WHEN Copia_Cedible_sf IS NOT NULL THEN 1 END) as facturas_con_cedible_sf,
        COUNT(CASE WHEN Doc_Termico IS NOT NULL THEN 1 END) as facturas_con_doc_termico,
        COALESCE(
          AVG(
            (SELECT SUM(CAST(REGEXP_REPLACE(CAST(detalle.ValorTotal AS STRING), '[^0-9.-]', '') AS FLOAT64))
             FROM UNNEST(detallesFactura) AS detalle 
             WHERE detalle.ValorTotal IS NOT NULL 
             AND REGEXP_CONTAINS(CAST(detalle.ValorTotal AS STRING), r'^-?[0-9]+\.?[0-9]*$'))
          ), 0) as valor_promedio_facturas,
        COALESCE(
          SUM(
            (SELECT SUM(CAST(REGEXP_REPLACE(CAST(detalle.ValorTotal AS STRING), '[^0-9.-]', '') AS FLOAT64))
             FROM UNNEST(detallesFactura) AS detalle 
             WHERE detalle.ValorTotal IS NOT NULL 
             AND REGEXP_CONTAINS(CAST(detalle.ValorTotal AS STRING), r'^-?[0-9]+\.?[0-9]*$'))
          ), 0) as valor_total_rango
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
      WHERE
        fecha >= @start_date AND fecha <= @end_date
      GROUP BY DATE(fecha)
      ORDER BY fecha_factura DESC
      LIMIT 100
    description: |
      Obtiene estadísticas detalladas de facturas dentro de un rango de fechas específico.
      Agrupa los resultados por fecha y muestra métricas comprensivas incluyendo:
      - Conteos de facturas, RUTs, solicitantes y clientes únicos por día
      - Disponibilidad de diferentes tipos de PDFs por día
      - Valores promedio y totales de facturas por día
      
      Útil para análisis de actividad de facturación por períodos específicos.
      Ejemplo: "Estadísticas de facturas entre enero y marzo 2019"
    parameters:
      - name: start_date
        type: string
        description: Fecha de inicio en formato YYYY-MM-DD (ej. "2019-01-01")
        required: true
      - name: end_date
        type: string
        description: Fecha de fin en formato YYYY-MM-DD (ej. "2019-12-31")
        required: true

  get_data_coverage_statistics:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        MIN(fecha) as Fecha_Inicio,
        MAX(fecha) as Fecha_Fin,
        COUNT(DISTINCT Rut) as Total_RUTs_Unicos,
        COUNT(*) as Total_Facturas,
        COUNT(DISTINCT EXTRACT(YEAR FROM fecha)) as Anos_Cubiertos,
        COUNT(DISTINCT EXTRACT(MONTH FROM fecha)) as Meses_Distintos,
        ROUND(AVG(EXTRACT(YEAR FROM fecha)), 1) as Ano_Promedio
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
    description: |
      Obtiene estadisticas completas del horizonte temporal y cobertura de datos.
      Usar cuando el usuario pregunte sobre rango temporal de datos o despues de mostrar estadisticas de RUTs.
      Proporciona contexto temporal completo del dataset de facturas.
      Incluye fechas de inicio/fin, total de RUTs unicos, facturas totales, anos cubiertos y estadisticas temporales.

  get_yearly_invoice_statistics:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        EXTRACT(YEAR FROM fecha) as Ano,
        COUNT(*) as Total_Facturas,
        COUNT(DISTINCT Rut) as RUTs_Distintos,
        COUNT(DISTINCT Solicitante) as Solicitantes_Distintos,
        MIN(fecha) as Primera_Factura,
        MAX(fecha) as Ultima_Factura,
        ROUND(COUNT(*) * 100.0 / (SELECT COUNT(*) FROM `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`), 2) as Porcentaje_Total,
        COALESCE(
          SUM(
            (SELECT SUM(detalle.ValorTotal)
             FROM UNNEST(DetallesFactura) AS detalle 
             WHERE detalle.ValorTotal IS NOT NULL)
          ), 0) as Valor_Total_Ano
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
      GROUP BY EXTRACT(YEAR FROM fecha)
      ORDER BY Ano ASC
    description: |
      Obtiene el desglose completo de facturas por año con estadísticas detalladas.
      ESENCIAL para responder preguntas como "cuántas facturas hay por cada año" o "desglose anual".
      Incluye:
      - Número de facturas por año
      - RUTs y solicitantes únicos por año
      - Rango temporal (primera y última factura) de cada año
      - Porcentaje que representa cada año del total
      - Valor total monetario por año
      
      Usar cuando el usuario solicite distribución temporal anual de facturas.

  get_monthly_invoice_statistics:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        year_num as Ano,
        month_num as Mes,
        CASE month_num
          WHEN 1 THEN 'Enero'
          WHEN 2 THEN 'Febrero'
          WHEN 3 THEN 'Marzo'
          WHEN 4 THEN 'Abril'
          WHEN 5 THEN 'Mayo'
          WHEN 6 THEN 'Junio'
          WHEN 7 THEN 'Julio'
          WHEN 8 THEN 'Agosto'
          WHEN 9 THEN 'Septiembre'
          WHEN 10 THEN 'Octubre'
          WHEN 11 THEN 'Noviembre'
          WHEN 12 THEN 'Diciembre'
        END as Nombre_Mes,
        COUNT(*) as Total_Facturas,
        COUNT(DISTINCT Rut) as RUTs_Distintos,
        COUNT(DISTINCT Solicitante) as Solicitantes_Distintos,
        MIN(fecha) as Primera_Factura_Mes,
        MAX(fecha) as Ultima_Factura_Mes
      FROM (
        SELECT 
          *,
          EXTRACT(YEAR FROM fecha) as year_num,
          EXTRACT(MONTH FROM fecha) as month_num
        FROM `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
        WHERE EXTRACT(YEAR FROM fecha) = @target_year
      )
      GROUP BY year_num, month_num
      ORDER BY month_num ASC
    description: |
      Obtiene el desglose completo de facturas por mes dentro de un año específico.
      ESENCIAL para responder preguntas como "cuántas facturas hay por mes en 2025" o "desglose mensual de 2024".
      Incluye:
      - Número de facturas por mes
      - RUTs y solicitantes únicos por mes
      - Rango temporal (primera y última factura) de cada mes
      - Nombres de meses en español
      
      Usar cuando el usuario solicite distribución mensual de facturas dentro de un año específico.
    parameters:
      - name: target_year
        type: integer
        description: Año para el cual obtener estadísticas mensuales (ej. 2025, 2024)
        required: true

  search_invoices_by_company_name_and_date:
    kind: bigquery-sql
    source: gasco_invoices_read
    statement: |
      SELECT
        Factura,
        Solicitante,
        Rut,
        Nombre,
        fecha,
        DetallesFactura,
        CASE WHEN Copia_Cedible_cf IS NOT NULL 
             THEN Copia_Cedible_cf 
             ELSE NULL END as Copia_Cedible_cf_proxy,
        CASE WHEN Copia_Cedible_sf IS NOT NULL 
             THEN Copia_Cedible_sf 
             ELSE NULL END as Copia_Cedible_sf_proxy,
        CASE WHEN Copia_Tributaria_cf IS NOT NULL 
             THEN Copia_Tributaria_cf 
             ELSE NULL END as Copia_Tributaria_cf_proxy,
        CASE WHEN Copia_Tributaria_sf IS NOT NULL 
             THEN Copia_Tributaria_sf 
             ELSE NULL END as Copia_Tributaria_sf_proxy,
        CASE WHEN Doc_Termico IS NOT NULL 
             THEN Doc_Termico 
             ELSE NULL END as Doc_Termico_proxy
      FROM
        `datalake-gasco.sap_analitico_facturas_pdf_qa.pdfs_modelo`
      WHERE
        (UPPER(Solicitante) LIKE CONCAT('%', UPPER(@company_name), '%') 
         OR UPPER(Nombre) LIKE CONCAT('%', UPPER(@company_name), '%'))
        AND EXTRACT(YEAR FROM fecha) = @year
        AND EXTRACT(MONTH FROM fecha) = @month
      ORDER BY fecha DESC, Factura DESC
      LIMIT 1000
    description: |
      Busca facturas por nombre de empresa/solicitante/cliente y mes/año específicos.
      ESENCIAL para consultas como "facturas de Gas las Naciones en julio 2025".
      
      Características:
      - Busca en AMBOS campos: Solicitante (proveedor) Y Nombre (cliente)
      - Normaliza automáticamente a MAYÚSCULAS para coincidencia
      - Acepta nombres parciales: "gas naciones" encontrará "GAS LAS NACIONES"
      - Filtra por mes y año específicos
      - Incluye todas las rutas de PDFs disponibles
      
      Usar cuando el usuario mencione empresa + período temporal específico.
    parameters:
      - name: company_name
        type: string
        description: Nombre de la empresa, cliente o solicitante (ej. "Gas las Naciones", "Agrosuper")
      - name: year
        type: integer
        description: Año de las facturas (ej. 2025, 2024)
      - name: month
        type: integer
        description: Mes de las facturas (1-12, donde 7=julio)

toolsets:
  gasco_invoice_search:
    - search_invoices
    - search_invoices_by_date
    - search_invoices_by_rut
    - search_invoices_by_date_range
    - search_invoices_by_rut_and_date_range
    - get_solicitantes_by_rut
    - search_invoices_by_month_year
    - search_invoices_by_multiple_ruts
    - search_invoices_recent_by_date
    - search_invoices_by_solicitante_and_date_range
    - get_unique_ruts_statistics
    - search_invoices_by_rut_and_amount
    - get_date_range_statistics
    - search_invoices_by_proveedor
    - search_invoices_by_company_name_and_date
    - search_invoices_by_factura_number
    - search_invoices_by_referencia_number
    - search_invoices_by_any_number
    - search_invoices_by_cliente
    - search_invoices_by_minimum_amount
    - get_invoice_statistics
    - get_monthly_invoice_statistics
    - get_invoices_with_pdf_info
    - get_invoices_with_proxy_links
    - get_invoices_with_all_pdf_links
    - get_multiple_pdf_downloads
    - get_cedible_cf_by_solicitante
    - get_cedible_sf_by_solicitante
    - get_tributaria_cf_by_solicitante
    - get_tributaria_sf_by_solicitante
    - get_tributarias_by_solicitante
    - get_cedibles_by_solicitante
    - get_data_coverage_statistics
    - get_yearly_invoice_statistics

  gasco_zip_management:
    - create_zip_record
    - list_zip_files
    - get_zip_info
    - update_zip_status
    - record_zip_download
    - get_zip_statistics
