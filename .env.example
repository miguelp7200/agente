# ================================================================
# Invoice Backend - Environment Variables Configuration
# ================================================================
# This file documents all available environment variables for configuration.
# Environment variables override values from config/config.yaml.
# Copy this file to .env and customize for your environment.
# ================================================================

# ================================================================
# Google Cloud Service Accounts
# ================================================================
# PDF_SIGNER_SERVICE_ACCOUNT=adk-agent-sa@agent-intelligence-gasco.iam.gserviceaccount.com
# Service account for signing GCS URLs with impersonation

# ================================================================
# BigQuery Configuration
# ================================================================
# BIGQUERY_TIMEOUTS_QUERY_DEADLINE=60.0
# Query deadline in seconds for BigQuery retry operations

# ================================================================
# GCS (Google Cloud Storage) - Time Synchronization
# ================================================================
# GCS_TIME_SYNC_THRESHOLD_SECONDS=60
# Maximum acceptable time difference in seconds between local and GCS

# GCS_TIME_SYNC_CHECK_TIMEOUT=5
# Timeout in seconds for time synchronization check

# ================================================================
# GCS - Buffer Time by Clock Skew Status (minutes)
# ================================================================
# GCS_BUFFER_TIME_CLOCK_SKEW_DETECTED=5
# Buffer time when clock skew is detected

# GCS_BUFFER_TIME_VERIFICATION_FAILED=3
# Buffer time when time verification fails

# GCS_BUFFER_TIME_SYNCHRONIZED=1
# Buffer time when time is properly synchronized

# ================================================================
# GCS - Retry Logic
# ================================================================
# GCS_RETRY_MAX_RETRIES=3
# Maximum number of retries for GCS operations

# GCS_RETRY_BASE_DELAY_SECONDS=60
# Base delay in seconds for exponential backoff

# GCS_RETRY_MAX_DELAY_SECONDS=300
# Maximum delay in seconds for exponential backoff

# GCS_RETRY_BACKOFF_MULTIPLIER=2.0
# Multiplier for exponential backoff calculation

# GCS_RETRY_REQUEST_TIMEOUT=30
# HTTP request timeout in seconds for GCS operations

# GCS_RETRY_JITTER_ENABLED=true
# Enable jitter in retry delays to avoid thundering herd

# ================================================================
# GCS - Download Configuration
# ================================================================
# GCS_DOWNLOAD_TIMEOUT_LARGE_FILES=60
# Timeout in seconds for downloading large files from GCS

# GCS_DOWNLOAD_MAX_CONCURRENT=10
# Maximum number of concurrent downloads

# ================================================================
# PDF & Signed URLs
# ================================================================
# PDF_SIGNED_URLS_EXPIRATION_HOURS=24
# Default expiration time for signed URLs in hours

# PDF_SIGNED_URLS_MAX_EXPIRATION_HOURS=168
# Maximum allowed expiration (1 week = 168 hours)

# ================================================================
# Validation Limits
# ================================================================
# VALIDATION_MAX_URL_LENGTH=2000
# Maximum length for regular URLs in characters

# VALIDATION_MAX_ZIP_URL_LENGTH=3000
# Maximum length for ZIP download URLs in characters

# VALIDATION_SIGNATURE_MAX_LENGTH=500
# Maximum length for URL signatures

# VALIDATION_SIGNATURE_PATTERN_CHECK_LENGTH=50
# Length for signature pattern validation

# ================================================================
# Vertex AI Configuration
# ================================================================
# VERTEX_AI_THINKING_MAX_BUDGET=8192
# Maximum thinking budget for Vertex AI (reasoning tokens)

# VERTEX_AI_MODEL=gemini-2.5-flash
# Vertex AI model to use

# VERTEX_AI_TEMPERATURE=0.3
# Temperature for model responses (0.0-1.0)

# ================================================================
# Notes
# ================================================================
# - All numeric values are strings and will be cast by the application
# - Boolean values should be "true" or "false" (lowercase)
# - Environment variables take precedence over config.yaml values
# - Overrides are logged at DEBUG level for troubleshooting
# - Most values have sensible defaults and don't require customization
# ================================================================

# ================================================================
# Example: Override service account for different project
# ================================================================
# PDF_SIGNER_SERVICE_ACCOUNT=my-service-account@my-project.iam.gserviceaccount.com

# ================================================================
# Example: Increase timeouts for slow connections
# ================================================================
# BIGQUERY_TIMEOUTS_QUERY_DEADLINE=120.0
# GCS_RETRY_REQUEST_TIMEOUT=60
# GCS_DOWNLOAD_TIMEOUT_LARGE_FILES=120
