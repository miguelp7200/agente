# Configuración de Variables de Entorno - Producción Cloud Run
# Basado en el .env original pero optimizado para Cloud Run

# Google Cloud Platform - Arquitectura Dual (PRODUCCIÓN)
# LECTURA: Datos de producción Gasco (solo lectura)
GOOGLE_CLOUD_PROJECT_READ=datalake-gasco
BIGQUERY_DATASET_READ=sap_analitico_facturas_pdf_qa
BUCKET_NAME_READ=miguel-test

# ESCRITURA: Operaciones del agente (ZIPs, logs)
GOOGLE_CLOUD_PROJECT_WRITE=agent-intelligence-gasco
BIGQUERY_DATASET_WRITE=zip_operations
BUCKET_NAME_WRITE=agent-intelligence-zips

# Service Account Names (producción Cloud Run)
SERVICE_ACCOUNT_MCP=mcp-toolbox-sa@agent-intelligence-gasco.iam.gserviceaccount.com
SERVICE_ACCOUNT_FILE=file-service-sa@agent-intelligence-gasco.iam.gserviceaccount.com
SERVICE_ACCOUNT_ADK=adk-agent-sa@agent-intelligence-gasco.iam.gserviceaccount.com

# Configuración regional (compartida)
GOOGLE_CLOUD_LOCATION=us-central1

# Vertex AI - Usar Service Account en Cloud Run
GOOGLE_GENAI_USE_VERTEXAI=true

# LangExtract configuración optimizada para producción
LANGEXTRACT_MODEL=gemini-2.5-flash
LANGEXTRACT_TEMPERATURE=0.1
LANGEXTRACT_MAX_WORKERS=5

# Debugging (DESHABILITADO en producción)
DEBUG_MODE=false
LOG_LEVEL=INFO

# Configuración de empaquetado ZIP
# Max 2 facturas (4 PDFs) due to signed URL timeout constraints
ZIP_THRESHOLD=2
ZIP_PREVIEW_LIMIT=3
ZIP_EXPIRATION_DAYS=7

# Límites de visualización en el chat
MAX_PDF_LINKS_DISPLAY=10
PDF_SERVER_PORT=8011

# Configuración de API y MCP Toolbox (Cloud Run interno)
PORT=8080
MCP_TOOLBOX_URL=http://localhost:5000

# Cloud Run específico
CLOUD_RUN_ENVIRONMENT=production
CLOUD_RUN_REGION=us-central1
CLOUD_RUN_PROJECT=agent-intelligence-gasco

# Security headers
SECURE_HEADERS=true
CORS_ORIGINS=*

# Performance tuning
MAX_CONCURRENT_REQUESTS=100
REQUEST_TIMEOUT=300

# Nota importante para Cloud Run:
# - No usar GOOGLE_APPLICATION_CREDENTIALS (se asigna automáticamente)
# - Service Accounts configuradas por despliegue
# - Usar Application Default Credentials (ADC)
